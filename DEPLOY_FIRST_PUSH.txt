FIRST DEPLOY / FIRST PUSH

1) Push this repo to GitHub.
2) Let GitHub Actions run workflow: daily-refresh.
3) Wait until workflow finishes and commits refreshed universe + scanner snapshots.
4) Redeploy Streamlit if needed.

Important:
- This repo is snapshot-first. On first deploy, app may look empty until the workflow finishes.
- After the workflow commits refreshed artifacts, the app becomes populated without manual upload of data files.
- Scanner path: scanner_pro/app.py
- Macro path: macroregime_pro/app.py
