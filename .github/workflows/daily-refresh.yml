name: daily-refresh-note

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Builder note
        run: |
          echo "Full-universe no-cut builds are intentionally NOT run on GitHub-hosted runners in this pack."
          echo "Run build_daily_local.py on a local machine or VPS with persistent storage, then commit updated universe/snapshot files."
